services:
  podcast-server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    environment:
      - DB_PATH=/data/podcast.db
      - PORT=8080
    restart: unless-stopped